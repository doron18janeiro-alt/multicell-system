import * as clientes from "@/services/clientes";

export const listClients = clientes.listClients;
export const getClientById = clientes.getClientById;
export const createClient = clientes.createClient;
export const updateClient = clientes.updateClient;
export const deleteClient = clientes.deleteClient;
