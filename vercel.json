{
  "buildCommand": "cd multicell-system && npm install && npm run build",
  "outputDirectory": "multicell-system/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
